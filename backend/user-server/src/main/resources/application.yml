spring:
  # database local
  datasource:
    url: jdbc:mysql://localhost:3306/ad_db
    username: root
    password: root

    jpa:
      hibernate:
        ddl-auto: create
      show-sql: true
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.MySQL8Dialect
    logging:
      level:
        org.hibernate.type: trace

oauth2:
  google:
    clientId: 409728357789-pfbb5h8u9m4tib34v3qne7n7atat1tbc.apps.googleusercontent.com
    redirect_uri: http://localhost:8080/oauth2/login/google


